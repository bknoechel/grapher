import React from 'react';

function About() {
  return (
    <div>
      <p>
        Grapher is a sample web app written with React and Bootstrap.
        It can plot simple tabular data in a line graph.
      </p>
    </div>
  );
}

export default About;
